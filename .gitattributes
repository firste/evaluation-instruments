# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Explicitly declare text files you want to always be normalized and converted
# to native line endings on checkout.
#*.c text

# Declare files that will always have LF line endings on checkout.
*.sh text eol=lf
.bashrc text eol=lf

# Track common data file types with Git LFS
*.[Tt][Ss][Vv] filter=lfs diff=lfs merge=lfs
*.[Cc][Ss][Vv] filter=lfs diff=lfs merge=lfs

# Track common model file types with Git LFS
*.[Pp][Tt][Hh] filter=lfs diff=lfs merge=lfs -text
*.[Pp][Ii][Cc][Kk][Ll][Ee] filter=lfs diff=lfs merge=lfs -text
*.[Hh]5 filter=lfs diff=lfs merge=lfs -text
